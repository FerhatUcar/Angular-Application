import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'webshop',
    templateUrl: 'webshop.html',
})

export class WebshopComponent  {
    constructor() {}
}
